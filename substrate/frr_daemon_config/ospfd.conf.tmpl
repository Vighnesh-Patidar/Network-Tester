! ospfd configuration for node {{NODE_ID}}
hostname {{NODE_ID}}
!
router ospf
 ospf router-id {{ROUTER_ID}}
 redistribute connected
{{OSPF_NETWORKS}}
!
! Faster timers keep convergence measurements bounded in CI while staying within
! safe operational ranges; tune alongside convergence_timeout_ms.
line vty
!
