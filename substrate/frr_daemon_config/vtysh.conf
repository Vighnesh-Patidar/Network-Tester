! vtysh configuration shared by every namespace. The chaos engine shells out to
! vtysh for JSON state (show ip ospf neighbor json, show ip route json, show bgp
! summary json), so vtysh must be able to reach each daemon's control socket.
service integrated-vtysh-config
!
